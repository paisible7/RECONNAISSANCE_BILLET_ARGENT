import 'package:flutter/material.dart';

class AppColors {
  // Palette avec fond BLANC
  
  // Arrière-plans - Blanc
  static const Color backgroundLight = Color(0xFFFFFFFF);     // Blanc pur
  static const Color backgroundCard = Color(0xFFF8F9FA);      // Gris très clair
  static const Color backgroundElevated = Color(0xFFF0F2F5);  // Gris léger

  // Primaire - Noir (pour contraste sur fond blanc)
  static const Color primary = Color(0xFF1A1A1A);             // Noir doux
  static const Color primaryLight = Color(0xFF333333);        // Gris foncé

  // Secondaire - Doré
  static const Color secondary = Color(0xFFD4AF37);           // Or classique
  static const Color secondaryLight = Color(0xFFE8C872);      // Or clair
  static const Color secondaryDark = Color(0xFFAA8B2C);       // Or foncé

  // Accent - Turquoise
  static const Color accent = Color(0xFF00B4B4);              // Turquoise
  static const Color accentLight = Color(0xFF00D4D4);         // Turquoise clair
  static const Color accentDark = Color(0xFF008888);          // Turquoise foncé

  // Texte
  static const Color textDark = Color(0xFF1A1A1A);            // Texte principal
  static const Color textGray = Color(0xFF6B7280);            // Texte secondaire
  static const Color textLight = Color(0xFFFFFFFF);           // Texte sur fond sombre

  // Bordures
  static const Color borderColor = Color(0xFFE5E7EB);
  static const Color borderDark = Color(0xFFD1D5DB);
}