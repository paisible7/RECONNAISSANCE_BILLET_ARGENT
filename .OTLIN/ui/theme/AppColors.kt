package com.ningapi.ui.theme

import androidx.compose.ui.graphics.Color

object AppColors {
    // Palette avec fond BLANC
  
    // Arrière-plans - Blanc
    val BackgroundLight = Color(0xFFFFFFFF)     // Blanc pur
    val BackgroundCard = Color(0xFFF8F9FA)      // Gris très clair
    val BackgroundElevated = Color(0xFFF0F2F5)  // Gris léger

    // Primaire - Noir (pour contraste sur fond blanc)
    val Primary = Color(0xFF1A1A1A)             // Noir doux
    val PrimaryLight = Color(0xFF333333)        // Gris foncé

    // Secondaire - Doré
    val Secondary = Color(0xFFD4AF37)           // Or classique
    val SecondaryLight = Color(0xFFE8C872)      // Or clair
    val SecondaryDark = Color(0xFFAA8B2C)       // Or foncé

    // Accent - Turquoise
    val Accent = Color(0xFF00B4B4)              // Turquoise
    val AccentLight = Color(0xFF00D4D4)         // Turquoise clair
    val AccentDark = Color(0xFF008888)          // Turquoise foncé

    // Texte
    val TextDark = Color(0xFF1A1A1A)            // Texte principal
    val TextGray = Color(0xFF6B7280)            // Texte secondaire
    val TextLight = Color(0xFFFFFFFF)           // Texte sur fond sombre

    // Bordures
    val BorderColor = Color(0xFFE5E7EB)
    val BorderDark = Color(0xFFD1D5DB)
}
